services:
  ov-bridge:
    build: ./services/ov-bridge
    network_mode: host
    volumes:
      - ./data:/data
    environment:
      LCM_URL: ${LCM_URL:-udpm://239.255.76.67:7667?ttl=1}
      SQLITE_PATH: /data/overwatch.db
      WS_PORT: ${WS_PORT:-7001}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    restart: unless-stopped

  ov-api:
    build:
      context: .
      dockerfile: services/ov-api/Dockerfile
    depends_on: [ov-bridge]
    volumes:
      - ./data:/data
    ports:
      - "${API_PORT:-3000}:3000"
    environment:
      SQLITE_PATH: /data/overwatch.db
      SESSION_SECRET: ${SESSION_SECRET}
      BRIDGE_WS_URL: ${BRIDGE_WS_URL:-ws://host.docker.internal:7001/events}
      ROBOT_MJPEG_URL: ${ROBOT_MJPEG_URL:-http://host.docker.internal:7001/video_feed/color_image}
      DEEP_LINK_SECRET: ${DEEP_LINK_SECRET}
      DEEP_LINK_TTL_HOURS: ${DEEP_LINK_TTL_HOURS:-24}
      DASHBOARD_BASE_URL: ${DASHBOARD_BASE_URL:-http://localhost:3001}
      LOG_LEVEL: ${LOG_LEVEL:-info}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  ov-telegram:
    build:
      context: .
      dockerfile: services/ov-telegram/Dockerfile
    depends_on: [ov-bridge, ov-api]
    volumes:
      - ./data:/data
    environment:
      SQLITE_PATH: /data/overwatch.db
      BRIDGE_WS_URL: ${BRIDGE_WS_URL:-ws://host.docker.internal:7001/events}
      MCP_URL: ${MCP_URL:-http://host.docker.internal:9990/mcp}
      PROVIDER: ${PROVIDER:-}
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      MODEL: ${MODEL:-}
      DASHBOARD_BASE_URL: ${DASHBOARD_BASE_URL:-http://localhost:3001}
      DEEP_LINK_SECRET: ${DEEP_LINK_SECRET}
      OV_API_URL: http://ov-api:3000
      LOG_LEVEL: ${LOG_LEVEL:-info}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  ov-dashboard:
    build:
      context: .
      dockerfile: services/ov-dashboard/Dockerfile
    depends_on: [ov-api]
    ports:
      - "${DASHBOARD_PORT:-3001}:3000"
    environment:
      OV_API_URL: http://ov-api:3000
      OV_API_PUBLIC_URL: ${OV_API_PUBLIC_URL:-http://localhost:3000}
      OV_ROBOT_MJPEG: ${OV_ROBOT_MJPEG:-http://host.docker.internal:7001/video_feed/color_image}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  # Caddy is opt-in via the `prod` profile so `make dev` (which expects
  # the dashboard on :3001 / api on :3000 directly) doesn't fight it for
  # ports 80/443. To enable TLS termination in production:
  #   docker compose --profile prod up -d
  caddy:
    image: caddy:2-alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy-data:/data
      - caddy-config:/config
    depends_on: [ov-api, ov-dashboard]
    profiles: ["prod"]
    restart: unless-stopped

volumes:
  caddy-data:
  caddy-config:
